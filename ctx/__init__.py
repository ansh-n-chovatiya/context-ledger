"""Context Ledger — durable specs, plans and memory for Claude Code sessions.

Stdlib only, on purpose: the plugin adds nothing to the host project's
dependency tree, so it can be installed globally and stay silent in projects
that have not opted in.
"""

__version__ = "0.4.0"
