---
description: Level, active work, briefing budget, recent journal
allowed-tools: Bash
---
!`"${CLAUDE_PLUGIN_ROOT}/bin/ctx" status || true`

Summarise only what the user would not already see above. Do not restate the table.
