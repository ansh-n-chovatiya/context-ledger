---
description: Return to L0 trace, keeping the journal
allowed-tools: Bash
---
!`"${CLAUDE_PLUGIN_ROOT}/bin/ctx" drop || true`

Acknowledge in one line. Journalling continues; nothing on disk was deleted.
